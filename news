Ventacompleta.py
Guardar el informe

    report_file = fields.Binary()

    def action_get_attachment(self):
        pdf = self.env.ref('TFG_Oscar.report_ventas_view')._render_qweb_pdf(self.ids)
        self.report_file = base64.b64encode(pdf[0])
        # save pdf as attachment
        name = "informe"
        return self.env['ir.attachment'].create({
            'name': name,
            'type': 'binary',
            'datas': self.report_file,
            'store_fname': name,
            'res_model': self._name,
            'res_id': self.id,
            'mimetype': 'application/x-pdf'
        })

ventacompleta_view

    <button name="action_get_attachment" string="Generate Attachment" type="object"/>

--------------------------------------------------------------------------------------------

SOLUCION SUMA INCORRECTA PRECIO NETO Y TOTAL
        VENTCOMPLETA.PY
    @api.depends('precioNeto')
    def _precioN(self):
        for rec in self:
            for ventas in self.env['ventar'].search([]):
                for ventas2 in rec.producto:
                    if ventas.idventa == ventas2.idventa:
                        rec.precioNeto += ventas.precioVenta2

-------------------------------------------------------------------------------------------------

INVISIBILIDAD REPORT
HAY QUE HACER INVISIBLE UN <t></t>

<t t-if="doc.hide == True"></t>

----------------------------------------------------------------------------------------------
SIGUIENTES PASOS
    AVERIGUAR EMAIL FACTURAS
    
